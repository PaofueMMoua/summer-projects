class Solution:
    def strStr(self, haystack: str, needle: str) -> int:
        
# get the haystack

# get the needle

# check the haystack
        # for i in haystack:
# check if the needle is in the haystack
    # find function?
        found = haystack.find(needle)
        if found >=0:
            print(needle)
            return found
        else:
            print(haystack)
            return -1

    # finding the needle in the haystack through potentially 


haystack = "sadbutsad"
needle = "sad"
# haystack = "leetcode"
# needle = "leeto"
# haystack = 
# needle = 
# haystack = 
# needle = 

response = Solution().strStr(haystack, needle)

print()
print()
print(response)