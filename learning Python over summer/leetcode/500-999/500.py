# class Solution:
#     def findWords(self, words: List[str]) -> List[str]:

# get the words list
# set the keyboard as list

words = ['wqerty', 'yuiop','rtyui']
firstKeyboardRow = ['q','w','e','r','t','y','u','i','o','p']
secondKeyboardRow = ['a','s','d','f','g','h','j','k','l']
thirdKeyboardRow = ['z','x','c','v','b','n','m']
        # 
tempWord = ''

# get the first index and compare it to see if every letter is equal to a letter in the same row.
    # use a for loop to cycle through each letter
for key in range(len(firstKeyboardRow)):
    if firstKeyboardRow[key] in words
    
        # compare each letter to the characters in each keyboard row doesnt matter if its uppercase or not.
        # compare using a 
        
        
        # r1="qwertyuiop"
        # r2="asdfghjkl"
        # r3="zxcvbnm"
        # res=[]
        # for word in words:
        #     w=word.lower()
        #     if len(set(r1+w))==len(r1) or len(set(r2+w))==len(r2) or len(set(r3+w))==len(r3) :
        #         res.append(word)
        # return res
    

# result = Solution.isIsomorphic(words)

# print()
# print()
# print()
# print()
# print(result)